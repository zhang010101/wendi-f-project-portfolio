# PR 案例配图清单

> 承接 `6.PR案例文案大纲.md`。本文档是文案落地前的配图准备清单——我直接用 Figma MCP 打开了 `2.PR.md`/`3.PR产品设计梳理.md` 里记录过的文件/节点，逐个截图核实，标出哪些已经可以直接导出用、哪些还没找到对应素材需要你帮忙定位。

---

## 已确认可用（我已截图核实内容，可以直接导出）

### 板块 3｜系统设计概览 —— 主视觉

**Functional Map**
- 文件：FigJam，fileKey `aQkJDd84KYn9Wi1MZNE1eU`，node `7:5350`
- 内容核实：清晰的角色泳道流程图（PL / Bauleiter / Architekt / Sekretariat / Subunternehmer / Polier），展示"Dokumente hochladen"这条主线里文档如何从各角色手里流转、审核、分发。图本身已经很干净，不需要重画，直接截图/导出即可。
- **建议**：导出这张图的高分辨率版本（我截的是 1400px 长边，正式使用建议导出到至少 2400px 长边或用 Figma 的 "Export" 功能出 2x PNG/SVG）。

**已确认（2026-08-15）**：板块3还新增了一张 **User Story Map** 配图，紧跟在 Functional Map 后面——用户直接截图发给我（不是通过 Figma MCP 导出），已存为 `public/images/pr-user-story-map.png`。内容是把整个模块按 Activities → Steps → Details 三层拆解，再按 P0/P1/P2 分 Release，直接支撑"分阶段交付"这个设计判断。

同时用户还发了另一张图 **Workflow - NEW**（FigJam `PR - map - flow` 文件，node `168-1012`），是"新建/上传文档"这一个功能点的详细分支流程（拖拽上传、逐个填属性 vs 批量填、加到已有文档 vs 新建文档等）。**这张图暂不用在板块3**（颗粒度太细，会拖慢板块3整体产品节奏），先留给板块4 Upload 可用性测试故事使用，可能能直接解决下面"多文档上传流程confusion"这个还没找到素材来源的问题。已先存为 `public/images/pr-upload-workflow-reference.png` 防止丢失，板块4 具体怎么用还需要展开确认。

### 板块 5｜系统性思维佐证 —— min/max width 响应式规范

- 文件：`PR-Mockups`，fileKey `tjxt0kdhKrFib5jyt8Ai5N`，node `895-16917`
- 内容核实：这张图本身就是一份带标注的响应式布局规范——用实际界面截图 + 箭头标注了 Filter area（默认 1/4 宽）、Records area（最小 422px）、Detail view（最小 400px，默认 1/3 宽）、File viewer（最小 560px）等每个面板的最小宽度和默认占比，一共展示了两种面板组合场景。**这是目前找到的、最直接能体现"系统性思维"的一张图**，几乎不用二次加工就能用。
- **建议**：直接导出，可能需要裁切/分成 2 张（两种场景各一张）以适配案例页面的图片比例。

### 板块 4｜深度案例主线 —— Upload 属性填写「实时校验反馈」前后对比

- 文件：`PR-Mockups`，fileKey `tjxt0kdhKrFib5jyt8Ai5N`
- **改进前**：node `740-53051`（"Preview + Attributs - Required fields: not completed"）—— 顶部黄色警告条"3/3 Dokumente: alle erforderlichen Felder sind nicht ausgefüllt"，Dokumenttyp 字段红框 + "Feld ist erforderlich"，缩略图上是红色感叹号标记
- **改进后**：node `740-52304`（"Preview + Attributs - Required field: completed"）—— 警告条消失，Dokumenttyp 已填"Image"，缩略图上变成绿色对勾
- 内容核实：这一组前后对比截图非常干净，**正好对应大纲里"加实时校验反馈"这条决策**，是板块4最适合当"决策"部分配图的素材——一张图就能让读者看懂问题和解法。
- **建议**：这两张图并排放（before/after），是板块4视觉冲击力最强的一组素材。

### 备选／可作补充（内容核实过，但和主线故事关联稍弱）

- **Indexing 批量套用元数据**：node `1973-93433`（同文件）——展示"这些文档已有值，是否要覆盖为统一值"的确认弹窗。对应板块3功能网格里提到的批量操作能力，如果板块4想额外提一句"批量效率"可以用，但不是两轮测试问题清单里直接提到的点，建议只作为板块3功能概览的备选缩略图，不强行塞进板块4。

---

## ~~还没找到对应素材，需要你帮忙定位~~

**已确认（2026-08-15）**：以下三个场景全部解决，素材来源不是 Figma，而是用户本地保存的可用性测试原始记录（两份截图：`PR Usability test_测试原话或反馈.jpg`、`PR Usability test_ 具体问题描述.jpg`）+ 一份新版 mockup 流程图（`Add new - new mockups.png`，32768×17174px 的 Figma 流程板导出图，本地文件不在 Figma 云端，直接从本地裁切）：

1. **单选项困惑** —— 对应 `Add new - new mockups.png` 里的三选一入口截图（Inbox / All files in one document / One document per file），已裁出存为 `public/images/pr-upload-choice.png`。
2. **Inbox 功能被误解** —— 和"单选项困惑"是同一张截图（Inbox 是三个选项之一），文字细节（"以为像 Docuware""以为是邮件"）来自 `PR Usability test_ 具体问题描述.jpg` 的 Inbox 分组。
3. **多文档上传流程问题** —— 对应 `Add new - new mockups.png` 里"合并成一个文档"结果（`pr-upload-all-in-one.png`）+ 属性校验警告/通过前后对比（`pr-upload-validation-warning.png`、`pr-upload-validation-resolved.png`）。

原来担心整张 mockup 大图直接缩小会导致所有 popup 看起来一样（用户 2026-08-15 提出的顾虑，见对话记录）——解决方式是从大图里裁出这几个独立场景，分别保存成单独的图片文件，而不是整图缩放。

另外还配了一段 73 秒操作录屏（`upload document.mov`，用户提供），用 `ffmpeg -c:v copy -c:a aac -movflags +faststart` 转封装成 `public/videos/pr-upload-demo.mp4`（原始 .mov 的 moov atom 在文件尾部，Chrome 的 `<video>` 元素没法边下载边播放，转成 faststart mp4 后解决）。

---

## 板块 1（Hero）—— 已确认可用

**Document grid（三栏主界面）**
- 文件：`PR - to dev`，fileKey `QijNgSwjUjvCbhWcUnwx9f`，node `709-3159`
- 内容核实：三栏经典布局——左侧 Structure 树状结构（Abschnitt 1 → A-House A → A.1F-First Floor，正好体现板块5提到的"贴合建筑工程行业层级"的文件夹结构）、中间文档表格（File type/Document title/Document type/Document date/Origin 等字段）、右侧 Detail Header 预览面板（多文件缩略图 + 大图预览 + Preview/Basic/Dokumentklasse/System infos/Teilen 多 tab）。
- **建议**：这张图信息量大、完整度高，是最适合做 Hero 主图或板块3功能概览封面的"一图看懂产品"素材，建议直接用作 Hero。

## 板块 2（背景与挑战）——建议不用产品截图

这块内容是"数据孤岛"的结构性论述（文档分散在7-8个专业系统），**不建议用产品截图**，更适合做一张示意图（比如"7个系统 → 1个 PR"的简单信息图）——这个我可以后续帮你在案例页面里用代码/SVG 直接画，不需要额外找 Figma 素材。

---

## 下一步

1. 你确认板块4三个"还没找到"的场景素材来源（Figma node / 测试工具导出 / 本地文档），我再去核实截图。
2. 素材清单定下来后，进入文案初稿阶段（新建 `8.PR案例文案初稿.md`），把 `6.PR案例文案大纲.md` 逐块扩写成正式文案，图片位置先用清单里的这些截图占位标注。
