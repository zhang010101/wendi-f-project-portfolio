# PR 案例作品集展示框架

> 承接 `1.brainstorming.md`/`1.brainstorming2.md`（筛选框架、候选素材清单）、`2.PR.md`/`2(2).数据收集方法.md`/`3.PR产品设计梳理.md`（PR 产品 Figma 素材深挖）。本文档是素材梳理到"如何讲这个产品"的转折点——设计细节量太大，不能全部塞进案例，需要一个取舍框架。产出于两块新素材之后：Upload 两轮可用性测试、"Product ANALYSIS" 竞品分析板（后者核实后确认与 PR 无关，已排除）。

---

## 一、叙事策略

产品定位一句话先立住：**不是通用文件管理工具，是贴合基建/工程项目管理工作流 + 生态互联的项目空间模块**（mydocma Projektraum-Modul），分阶段迭代交付（Volumen 1/2/3，来自 User Story Map）。

因为素材量极大，结构必须做减法：**背景概览（广度）+ 1条主线深挖（深度）+ 若干系统性思维佐证**，而不是把7个功能区块逐一展开讲。

---

## 二、建议的案例结构（七个板块，2026-08-21 新增板块4"角色视角"后由六个板块扩为七个）

### 1. Hero / 概览
- 产品名 + 一句话定位
- 角色说明：是否独立负责、和谁协作——测试记录里 Elisa 和 Wendi.F 共同参与，可以体现跨职能协作
- 关键信息条：时间跨度、平台

### 2. 背景与挑战 ⚠️ 素材缺口
目前**这是最大的素材空缺**——现有素材里没有"上线前最初的用户痛点/需求背景"第一手证据（客户求早鸟邮件说明的是"上线后需求强烈"，不是"上线前的问题"）。

**待补充**：当初为什么要做这个 Projektraum 模块，最早发现的用户痛点是什么。

### 3. 系统设计概览
- 多角色协作模型：**不做8个角色卡片式罗列**，改用 Functional Map（FigJam: `aQkJDd84KYn9Wi1MZNE1eU`, node `7:5350`）的截图/简化重绘版作为主视觉，展示 PL/BL/Architekt/Subunternehmer/Polier 等角色间任务如何衔接、文档如何跨角色流动；配 2-3 句文字点出图里最能体现设计难度的关系（例如同一份图纸数据要同时服务办公室精细操作和工地平板极简交互），文字里可以自然带出 2-3 个角色名字，但不单独展开成板块。角色细节背景见 `5.PR产品背景与角色梳理.md` 三、多角色协作模型。
- 分阶段交付节奏：User Story Map 的 Volumen 1/2/3
- 7大已上线功能区块做成一个概览清单/网格（不深挖，展示广度，Dashboard 也在这里带一笔即可，不在板块6单独展开；2026-08-20 新增 Filter & Search、标记删除 Delete 两项，见下方"已确认"第5点）
- **新增第三个设计判断：权限方向定了但特意不做**——业主/分包商这类外部角色"只应看到部分文档"的需求，加上"按过滤器绑角色权限、OR连接"的设计方向，在早期就已讨论清楚，但权限系统本身被主动排进 v1 范围之外。素材来源：`PR (Projektraum) - Basic info & User stories.docx` 会议纪要，详见 `5.PR产品背景与角色梳理.md` 九。

### 4. 这套系统对谁改变了什么（已确认，2026-08-21 新增）

**已确认（2026-08-21）**：采纳「9.项目叙事方法参考.md」提出的"方法六角色视角"强化——用户明确要求把 `PR (Projektraum) - Personas (new).docx` 落地成一个独立板块，而不是继续只当背景参考素材。板块3的 Functional Map 讲的是"内部角色之间文档怎么流转"，这个新板块补一层容易被忽略的**外部**角色：

- **Bauherr（业主/发包方）**：外部投资方，没有工程背景，只想清楚知道进度/成本/风险，不想被拽进内部协作细节。"只给业主看该看的部分"这个诉求，直接对应板块3"权限：方向定了，特意不做"这个判断——诚实标注为方向已定、尚未上线，不过度宣称。
- **Behördenvertreter / Gutachter（监管方/审查人员）**：核心诉求是"所有变更能不能追溯"。这个需求 PR **已经在用**已上线功能回应——文档详情面板的不可编辑时间/操作人字段（上传/创建/最后修改的 at/by + 文档版本，来自 `5.PR产品背景与角色梳理.md` 五）虽然不是专门为审查场景设计的，却直接命中了这个角色的诉求。

不做成8角色逐条罗列（板块3已经决定不这么做），只挑这两个此前案例里完全没出现过的外部角色，用具体场景代替抽象描述——一个诚实呈现"还没做"，一个证明"已有功能恰好命中了未曾专门设计的场景"，两种诚实姿态本身就是判断力的体现。已落地到 `data/projects.ts` 的 `caseStudy.roles` 字段，渲染在 `app/work/[slug]/page.tsx` 里系统设计板块之后、深度案例主线之前（sheet 编号 03，原 03/04/05 依次后移为 04/05/06）。

### 5. 深度案例主线（建议：Upload 可用性测试迭代）

**问题**：两轮测试里发现的具体障碍
- 单选项困惑："3个选项让人困惑"、"以为点击 inbox 会切换视图"
- 属性填写困惑："不理解 ablegen 按钮为什么还是灰的"、"想要字段自动预填"
- Inbox 功能被误解："以为像 Docuware"、"以为是邮件发送方式"
- 多文档上传流程问题："以为点 ablegen 后所有文档都会上传"

**过程**：引用具体测试原话 + "X/N 参与者"量化表述，展示真实的研究方法论，而不是凭感觉改设计。

**决策**：针对每类问题的具体改动
- 单选项加说明文案并重新措辞
- 加实时校验反馈
- 支持多选批量上传
- attributes 步骤加"+"按钮直接加文件
- 字段改名（Tags→Document tags，System infos→Metadaten）

**结果**：第二轮测试里"4/4 参与者能顺利完成属性填写和上传，没有问题"——但同时诚实呈现第二轮又发现的新问题（比如多文档上传流程、attributes 步骤加文件的路径），避免把故事讲成"一次迭代就完美了"，这种诚实反而更可信。

**改进落地情况**（已确认）：测试里提出的改进建议大部分已经实施；少数影响不大、或还需要再测试确认的细节点，留到之后再改。写"结果"部分时按这个口径表述——不是"全部完美落地"，也不是"只是建议"，是"大部分落地，少量细节待定"。

### 6. 系统性思维佐证
- 响应式布局的 min/max width 规范（Records区/Detail view/Filter area/File viewer 各自的最小宽度和默认占比）——证明不只是画页面，还考虑了不同面板配置下的布局约束
- 文件夹树状结构（Abschnitt→House→Floor）体现贴合建筑工程行业层级的信息架构设计

### 7. 影响力与结果
- 客户主动求早鸟邮件（定性信号，已有，2026年7月 Wolfgang Schmid 写给 Herr Ripperda 的邮件）
- Upload 测试前后的可用性改善数据（来自板块5）
- 若后续要补更硬的量化数据，可以推进 `2(2).数据收集方法.md` 里提过的方案（产品埋点、对照组对比等）
- **Dashboard 不在本板块单独展开**（已确认）：板块3的功能概览清单里已经带了 Dashboard 一项，足够了。如果以后做独立的"数据仪表盘可视化"类别案例，再在那里深挖，避免和本案例重复。

其余已上线功能（Download/Share/文档查看器/Dashboard/文件夹结构）建议只在板块3做概览列举，不逐一深挖，避免案例失焦——除非后续认为其中某个也值得单独拉出来讲。

---

## 三、待决策/待补充的关键点

1. ~~主线故事是否用 Upload 可用性测试~~ **已确认**：就用 Upload 可用性测试迭代（两轮测试）作为板块4深度案例主线，用户 2026-08-12 拍板。
2. ~~板块2"背景与挑战"的素材缺口~~ **已确认**：由 `5.PR产品背景与角色梳理.md` 的产品定位 + 8类角色痛点这两节结构性证据填补，不需要额外回忆第一手时间线证据，现有材料已经能成文。
3. ~~Upload 测试的改进建议采纳情况~~ **已确认**：大部分已实施，少数影响不大或还需再测试确认的细节点留到之后再改。"结果"部分按这个口径写，见板块4正文。
4. ~~PR 自带的 Dashboard 是否在本案例里展示~~ **已确认**：只在板块3的7大功能概览清单里带一笔，不在板块6单独展开，避免和以后可能独立做的"数据仪表盘可视化"类别重复。
5. ~~新素材（`Basic info & User stories.docx` 会议纪要 + `Personas (new).docx`）是否补充进案例~~ **已确认（2026-08-20）**：`Personas (new).docx` 核实后和 `5.PR产品背景与角色梳理.md` 三、多角色协作模型的 8 角色表内容一致，不算新信息。`Basic info & User stories.docx` 的会议纪要是新证据，贡献了板块3的第三个设计判断（权限方向定了但特意不做）和两个新功能项（Filter & Search、标记删除 Delete）。已落地到 `data/projects.ts`。
6. ~~`Personas (new).docx` 是否要单独做成"方法六角色视角"板块~~ **已确认（2026-08-21）**：用户明确要求采纳，新增板块4"这套系统对谁改变了什么"，挑 Bauherr 和 Behördenvertreter/Gutachter 两个角色做具体场景描述。已落地到代码，见上方板块4正文和板块二标题的板块数更新（六→七）。

（角色画像的呈现方式也已确认：板块3不做8个角色卡片罗列，改用 Functional Map 可视化 + 简短文字；板块4挑 2 个此前未出现的外部角色单独展开，两种呈现方式互补不重复。）

---

## 下一步

所有决策点已确认完毕，七个板块已全部落地到 `data/projects.ts`（案例卡片信息）和 `app/work/[slug]/page.tsx`（案例详情页）。2026-08-20 追加了权限判断决策和两个功能项；2026-08-21 追加了板块4"角色视角"整节；均已落地到代码，无待办事项。
