# PR 案例文案大纲（整合稿）

> 承接 `4.PR案例展示框架.md`（六板块结构）+ `5.PR产品背景与角色梳理.md`（背景/角色/跨模块素材）。本文档把两者合并成一份可以直接照着写案例文案的大纲——按最终六个板块顺序，逐块给出"写这块时用什么素材、怎么组织"。**板块4的主线故事仍待最终确认**（用户要求最后再定），其余五块的素材和呈现方式已经收敛完毕，可以先动笔。

---

## 板块 1｜Hero / 概览

- 产品名 + 一句话定位：**Project Room（PR）——mydocma 生态里为建筑基建工程项目打造的集中式协作与文档管理平台**（不是通用文件管理工具，是贴合基建工程项目管理工作流 + 生态互联的项目空间模块）
- 角色说明：测试记录里 Elisa 和 Wendi.F 共同参与，体现跨职能协作
- 关键信息条：时间跨度、平台（分阶段迭代交付 Volumen 1/2/3，来自 User Story Map）

---

## 板块 2｜背景与挑战

**主叙事**（来自 5.二）：从"项目文档分散在 7-8 个专业系统里，没人能看到全貌"这个结构性问题出发。

- **问题起点**：建筑工程项目文档天然分散——现场检查用 AppBuilder、日报用 RP、缺陷记录用 MM、会议纪要用 JF、图纸用 PX……没有统一入口能看到项目文档全貌。这是"数据孤岛"（data silos）问题的具体表现，不是抽象说法。
- **谁受影响**：8 类角色里几乎每一类都提到"文档分散/找不到/格式不统一"是核心痛点，Projektleiter（项目负责人）明确把"Datenfragmentierung"（数据碎片化）列为首要挑战。
- **PR 的解法**：不是新建一个孤立文档库，而是做**接收端**——通过 PR-API/Files-API 主动对接各专业模块，自动同步文档进来，用户不用在多系统间来回找。
- **佐证信号**：客户主动求早鸟邮件（2026年7月 Wolfgang Schmid 写给 Herr Ripperda）可以作为"这个问题真实存在、需求强烈"的定性支撑，呼应板块6。

---

## 板块 3｜系统设计概览

三条并列的设计支柱：

1. **多角色协作模型**——主视觉用 Functional Map（FigJam `aQkJDd84KYn9Wi1MZNE1eU`, node `7:5350`）的截图/简化重绘版，展示 PL/BL/Architekt/Subunternehmer/Polier 等角色间任务如何衔接、文档如何跨角色流动。配 2-3 句文字点出图里最能体现设计难度的关系——例如同一份图纸数据要同时服务办公室精细操作（PL/Architekt，desktop）和工地平板极简交互（BL/Polier，tablet）。文字里自然带出 2-3 个角色名字即可，不做成独立板块，不逐条罗列 8 个角色卡片。
   - 背景支撑（不直接呈现，写文案时做取舍参考）：8 角色画像表（5.三）——每个角色的场景/痛点/目标/常用文档类型；两种组织方式并存（Structure 系统同步的项目结构 vs Folder 用户自定义文件夹）是一个值得一提的设计判断点。
2. **分阶段交付节奏**：User Story Map 的 Volumen 1/2/3。
3. **功能广度**：7 大已上线功能区块做成一个概览清单/网格（Upload/Download/Share/文档查看器/Dashboard/文件夹结构/……），不逐一深挖，只展示广度。Dashboard 在这里带一笔即可，不在板块6单独展开。
   - 可选加一句跨模块集成的具体例证（来自 5.四）：比如 AppBuilder 的检查照片同步到 PR 时，需要精确定义 uploaded by/created by 等字段在跨模块场景下的语义——比抽象说"做了 API 集成"更有说服力，一两句话点到即可，不展开讲字段设计细节。

---

## 板块 4｜深度案例主线 ✅ 已确认：Upload 可用性测试迭代

**已确认**（2026-08-12）：主线就用 Upload 可用性测试迭代（两轮测试）。四段结构：

- **问题**：单选项困惑、属性填写困惑（ablegen 按钮置灰）、Inbox 功能被误解（以为像 Docuware/邮件）、多文档上传流程问题
- **过程**：引用测试原话 + "X/N 参与者"量化表述
- **决策**：单选项加说明文案、加实时校验反馈、支持多选批量上传、attributes 步骤加"+"按钮、字段改名
- **结果**：第二轮"4/4 参与者顺利完成"，同时诚实呈现第二轮新发现的问题（多文档上传流程等）。改进落地情况：大部分已实施，少数细节留待之后

---

## 板块 5｜系统性思维佐证

- 响应式布局的 min/max width 规范（Records区/Detail view/Filter area/File viewer 各自最小宽度和默认占比）
- 文件夹树状结构（Abschnitt→House→Floor）体现贴合建筑工程行业层级的信息架构设计
- 可选补充（来自 5.七映射表）：AppBuilder 字段映射的语义设计（uploaded by/created by 在跨模块场景下的定义）、Structure vs Folder 双轨组织设计——如果板块3已经点到，这里可以不重复，视最终篇幅取舍

---

## 板块 6｜影响力与结果

- 客户主动求早鸟邮件（定性信号，2026年7月 Wolfgang Schmid → Herr Ripperda）
- Upload 测试前后的可用性改善数据（来自板块4）
- 若后续要补更硬的量化数据：`2(2).数据收集方法.md` 里的方案（产品埋点、对照组对比等）
- Dashboard 不在本板块单独展开——板块3的功能概览清单里已带一笔，足够；若以后做独立"数据仪表盘可视化"案例，再深挖

---

## 下一步

所有板块内容和主线故事都已确认。接下来把本大纲逐块扩写成正式案例文案，落地到 `data/projects.ts`（案例卡片信息）和 `app/work/[slug]/page.tsx`（案例详情页，目前占位）。
