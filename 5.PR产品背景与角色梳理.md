# PR 产品背景与角色梳理

> 承接 `4.PR案例展示框架.md`。本文档整理四份新补充的内部文档素材（来自 `/PR文件` 目录）：`Project room - description.docx`、`PR Personas.docx`、`Documents to Project room 跨产品.docx`、`File types in project room.docx`。这批素材直接补上了 `4.PR案例展示框架.md` 里标记的两个空缺——**板块2"背景与挑战"**（产品定位/为什么做）和**板块3"系统设计概览"**（多角色协作模型的深度证据）。

---

## 一、产品定位（来自 Project room - description）

**一句话定位**：Project Room（PR）是 mydocma 生态里为建筑工程项目打造的集中式协作与文档管理平台——统一存储、管理、（后续）讨论所有项目相关文档的结构化数字枢纽。

**核心目的**：消除数据孤岛（data silos）——把原本分散在各专业模块（AppBuilder 检查照片、PX 图纸、日报 PDF、缺陷记录……）里的文档，整合到一个所有项目干系人都能访问的统一位置，从项目经理到分包商，都能拿到最新信息。

**四大功能支柱**：
1. **统一文档存储（Unified Document Storage）**：中央仓库，支持一个 Dokument 可包含多个 File（复合文档）
2. **跨模块集成（Cross-Module Integration）**：与 mydocma 全家桶深度打通
3. **智能索引（Intelligent Indexing）**：每个文档都带元数据，通过 Filter Area + Dashboard 可搜索可筛选
4. **协作与分享（Collaboration & Sharing）**：可通过安全受限链接与外部伙伴分享文档

**两种组织方式并存**（这一点值得放进"系统设计概览"，是个设计判断点）：
- **Structure（结构）**：从 CO 模块同步过来的技术层级（项目结构），系统生成，用户不能改
- **Folder（文件夹）**：用户自定义的灵活文件夹树，手动组织

> 备注：截至 2026年7月，PR 尚未开发权限系统（"there are no permissions yet developed in PR"）——这是产品当前阶段的一个真实约束，如果案例里提到"权限/角色可见性"要注意这个时间点的准确性。

---

## 二、背景与挑战：为什么要做 PR（填补板块2空缺）

这批素材虽然不是"上线前的原始需求邮件"，但提供了**结构性的背景证据**，可以支撑板块2的论述：

- **问题起点**：建筑工程项目的文档天然分散在多个专业系统里（现场检查用 AppBuilder、日报用 RP、缺陷记录用 MM、会议纪要用 JF、图纸用 PX……），没有一个统一的地方能看到项目的全部文档全貌 → 这是"数据孤岛"问题的具体表现，不是抽象说法
- **谁受影响**：8 类角色（见下）里几乎每一类都提到"文档分散/找不到/格式不统一"是核心痛点，尤其是 Projektleiter（项目负责人）明确把"Datenfragmentierung"（数据碎片化）列为首要挑战
- **PR 的解法**：不是新建一个孤立的文档库，而是做**接收端**——通过 PR-API/Files-API 主动对接各专业模块的 API，把文档自动同步进来，用户不需要在多个系统之间来回找

这条线可以作为板块2"背景与挑战"的主叙事：**从"项目文档分散在7-8个专业系统里，没人能看到全貌"这个结构性问题出发，讲清楚 PR 为什么要存在**。

---

## 三、多角色协作模型（填补板块3深度，来自 PR Personas）

比此前 Functional Map 里的角色列表（PL/BL/Architekt/Subunternehmer/Polier）更细致，一共 **8 个角色**，每个都有场景、需求、痛点、目标、常用文档类型：

| 角色 | 场景 | 核心痛点 | 目标 | 常用文档类型 |
|---|---|---|---|---|
| **Projektleiter (PL)** 项目负责人 | 办公室 / desktop | 数据碎片化；变更版本难追踪 | 集中管理、定权限、找/开/分发文档；后续想要自动化通知（如新图纸版本） | 结算、合同、往来邮件 |
| **Bauleiter (BL)** 现场负责人 | 工地 / tablet | 时间紧迫，没空做办公室工作 | 快速查找/打开文档核实信息；后续想在 PR 里直接录入专业模块数据（如缺陷、照片） | 图纸、发票、往来邮件 |
| **Architekt / Planer** 建筑师/专业规划师 | 办公室 / desktop | 版本管理混乱，跨专业协作易出错 | 上传图纸（单个/批量）、核对版本；后续想要压缩包批量上传+自动版本号+新版本自动通知其他用户 | 图纸 |
| **Bauherr (AG)** 业主/发包方 | 办公室 / desktop，外部用户，只能看部分文档 | 非技术背景，需要清晰呈现 | 看项目全局概览（进度/成本/风险） | 日报、缺陷统计、往来邮件、审批文件 |
| **Polier** 工长 | 工地 / tablet | 软件不熟练，现场访问要求快 | 用 PR 快速打开图纸；易用性优先 | 图纸、BIM 模型 |
| **Subunternehmer** 分包商 | 办公/工地，desktop/tablet，用户群非常多样 | 文档要传给总包（GU），沟通成本高 | 上传要求的文档到指定文件夹；后续想要聊天沟通+接收任务 | 图纸、进度计划 |
| **Sekretariat** 行政助理 | — | 要管理的文档种类多、量大 | 归档、分发文档 | 几乎所有类型 |
| **Behördenvertreter / Gutachter** 监管方/审查人员 | 办公室 / desktop | 需要能追溯所有变更以保证审查结论准确 | 快速访问相关数据 | — |

**这张表对案例的价值**：
- 直接证明"多角色协作模型"不是纸面概念，每个角色都有明确的使用场景（办公室 vs 工地、desktop vs tablet）和差异化需求——这本身就是一个值得在案例里强调的**设计约束**：同一个产品要同时服务"坐在办公室看报表的项目负责人"和"戴着安全帽在工地用平板快速开图纸的工长"
- Bauherr/Subunternehmer 明确提到"只能看部分文档/自己公司的文档"——虽然权限系统还没做，但这是已经被识别、待实现的需求，可以在案例里诚实地标注为"已识别但未上线"

**呈现方式（已确认）**：案例里最终不会把这张表逐条展开。改用 Functional Map（FigJam: `aQkJDd84KYn9Wi1MZNE1eU`, node `7:5350`）的截图/简化重绘版作为板块3主视觉，展示角色间任务如何衔接、文档如何跨角色流动，配 2-3 句文字点出图里最能体现设计难度的关系，文字里可以自然带出 2-3 个角色名字（不需要纠结选哪几个），不做成独立的角色板块。第二张图（Personas-task，node `40000001:1615`）信息和上面这张表重叠，不需要额外引用。这张表本身作为背景参考留在本文档里即可。

---

## 四、跨模块集成全景（来自 Documents to Project room 跨产品）

补充系统设计的技术深度，证明"跨模块集成"不是空话，而是有具体字段级设计的：

**已上线连接**（截至 2026-01-15）：
- **AppBuilder → PR**：现场检查（inspection）里添加的照片会自动同步到 PR，每张照片在 PR 里是一个独立文档；AppBuilder 里删除已完成检查会连带删除 PR 里的照片
- **PR → PX**：PR 不主动推送，PX 反过来主动从 PR 拉取图片

**筹备中的连接**：JF（会议纪要）、AP（检查）、RP（日报 PDF，涉及签字版/审批版的区分）、MM（缺陷及其附件）

**AppBuilder 照片同步的字段设计示例**（体现设计的精细程度）：
- Title = 检查名称 + 图片序号；Date = 检查发送时间；Origin = AP；Uploaded at/by 和 Created at/by 之间有特殊定义规则（若来源是 AP，两者取值相同）
- 文件本身也带独立的 file date / file uploaded date / file uploaded by / file size，以及 AP 特有的 importedAt/importedBy
- 这套字段设计说明"一个文档从其他模块同步进 PR"背后有一整套关于"谁创建的/谁上传的/什么时候"的语义澄清工作

**这部分对案例的价值**：可以作为"系统设计概览"里"跨模块集成"这条支柱的具体例证——不需要展开讲，一两句话点出"比如 AppBuilder 的检查照片同步到 PR 时，需要精确定义 uploaded by/created by 等字段在跨模块场景下的语义"，比抽象说"做了 API 集成"更有说服力。

---

## 五、文档属性与文档类型体系

结合 `Document types in Project room.docx`（已在上一轮梳理）和 `Project room - description.docx` 的术语表，完整的文档元数据字段：

- Document Type / Subtype（必填，可全局或按项目配置启停）
- Document Title、Document Date、Document Number
- Company（关联公司）
- Tags / PX Tags（**尚未实现**）
- Uploaded/Created/Last edited 的 at/by 四组时间人员字段

**Settings-API 的职责范围**（说明"文档类型配置"只是模块设置能力的一部分）：定义 Document Classes 及自定义属性（后续）、管理 Document Types/Subtypes、存储用户/项目级 UI 设置（如已保存的网格视图/筛选偏好）、配置自动化通知和同步规则。

## 六、支持文件类型（参考性素材，来自 File types in project room）

按类型分类：image / pdf / word_document / excel / powerpoint / video / text / archive / email / **cad_drawing**（.dwg .dxf 等）/ **bim**（.rvt .ifc 等）/ code / visio / project / psd / ai / model_3d（.obj .fbx .stl）

**值得注意**：cad_drawing、bim、model_3d 这些工程专业格式的存在，呼应了"贴合建筑工程行业"的产品定位——不是一个通用文件管理器会优先支持的格式范围。多文件类型在一个文档里会显示 "multifiles" 图标；无法识别的类型显示 "unknown"。

---

## 七、对作品集板块的映射建议

| 板块（来自 `4.PR案例展示框架.md`） | 本轮素材的贡献 |
|---|---|
| 2. 背景与挑战 | ✅ 大幅填补——数据孤岛问题 + 8 类角色的痛点证据，可以写出有说服力的背景叙事了 |
| 3. 系统设计概览 | ✅ 补深——8 角色画像替代/补充原 Functional Map；两种组织方式并存（Structure vs Folder）的设计判断；跨模块集成的字段级例证 |
| 5. 系统性思维佐证 | 可选补充：AppBuilder 字段映射的语义设计、Structure vs Folder 的双轨组织设计 |

---

## 八、仍待补充/待确认

1. ~~板块2的背景叙事第一手时间线证据~~ **已确认**：不需要现在回忆补充，现有的产品定位文档 + 角色痛点这两节结构性证据已经足够支撑板块2成文。
2. ~~8 个角色里哪几个是案例里要重点提及的~~ **已确认**：不需要挑选，改用 Functional Map 图 + 简短文字呈现，见上方"三、多角色协作模型"末尾的呈现方式说明。
3. 本文档信息量较大，和 `4.PR案例展示框架.md` 的六板块结构如何最终整合成一篇完整案例文案，仍需下一步梳理——这是目前唯一还没做的整合工作。

---

## 下一步

素材补充和呈现方式的决策点已基本确认完毕（唯一还悬而未决的是 `4.PR案例展示框架.md` 里的主线故事，用户明确要求最后再定）。接下来可以进入"从头理顺"整个案例结构这一步，把 `4.PR案例展示框架.md` 的六板块框架和本文档的素材整合成一份连贯的案例文案大纲。
