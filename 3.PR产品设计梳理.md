# PR产品设计梳理 —— Figma 页面结构 + 本轮理解

> 承接 `2.PR.md`（Figma素材初步梳理、待补充清单）和 `数据收集方法.md`（Beta上线后的数据收集方案）。本文档记录新一轮对话内容：用户给出已上线的7大功能区块清单，并提供 `PR-Mockups` 文件里三个具体页面（Detailview / Add new / Folder）的链接，用 Figma MCP 的 `get_metadata` 直接读取每个页面下的 section 结构，再逐一澄清了几个命名不清楚的功能点。

---
我给的任务：
之前已经输入给你了设计之前做的准备工作。下面输入给你一些我做的设计，你了解一下我都为这个产品做了哪些大块的功能。
  已上线的主要功能区块有：Hochladen von Dokumenten
  Herunterladen von Dokumenten
  Teilen von Dokumenten
  weitere Grundfunktionen des Projektraums
  Dokumenten Viewer
  Dashboard
  Ordnerstruktur anlegen/verändern
  具体可以查看pr这个产品的figma设计文件’PR-Mockups‘里各个页面下各section的名称（帮我将各个页面下各section的名称用列表列出）：
  1.Detailview(在表格里选择一个文件后右侧出现的其详情区域）:https://www.figma.com/design/tjxt0kdhKrFib5jyt8Ai5N/PR-Mockups?node-id=542-59643
  2.Add new - Prototype & Mocups（上传新文件）：https://www.figma.com/design/tjxt0kdhKrFib5jyt8Ai5N/PR-Mockups?node-id=417-47532
  3.Folder（文件夹：在文件列表的左侧区域上半部分，是structure tab旁边的一个tab）: https://www.figma.com/design/tjxt0kdhKrFib5jyt8Ai5N/PR-Mockups?node-id=2168-10801
  上面是我为pr做的设计，输入给你，你大致有个印象，不用具体看每个设计，先从section名字判断做的内容，哪里不清楚可以标记出来，你再问我。

---

已上线功能清单（用户提供）

- Hochladen von Dokumenten（上传文档）
- Herunterladen von Dokumenten（下载文档）
- Teilen von Dokumenten（分享文档）
- weitere Grundfunktionen des Projektraums（项目空间的其他基础功能）
- Dokumenten Viewer（文档查看器）
- Dashboard
- Ordnerstruktur anlegen/verändern（创建/修改文件夹结构）


## 第一部分：三个页面的 section 列表

用 Figma MCP `get_metadata` 直接读取（文件：`PR-Mockups`，fileKey `tjxt0kdhKrFib5jyt8Ai5N`），按页面分列，保留原始顺序（含重复出现的同名 section，通常代表不同版本/迭代）。
三个页面的 section 提取结果：

### 1. Detailview 页（node-id 542-59643）—— 表格里选中一个文件后，右侧出现的详情区域
Detailview 页（表格选中文件后右侧详情区）——主要承载 Dokumenten Viewer + Teilen von Dokumenten + Herunterladen von Dokumenten 这三块。里面能看出：系统信息 tab（System info）、全屏预览（Fullscreen）、PDF viewer 的两种适配模式（"Fit to screen size" vs "Fit to screen width"）、分享功能经过了好几轮版本迭代（share (new logic - basic/complex) → Share - version 1 (to dev/new) → 最后正式命名为 Teilen）、下载入口（share & download、download (to dev)）。还有 File navigation problem 和 Attaching new files in the existed Dokument（追加文件，你说之后展开）。

- Detailview - System info
- Document with more files（×2）
- Fullscreen（×2）
- None
- Share/save/annotation
- Step 2
- share & download
- min / max width
- Detailview - Preview: Button 'An Breite anpassen'
- Detail view（frame）
- UI check
- no preview
- More Details（frame）
- Insert drive file（frame）
- UI check - Preview
- ZIP
- 360 Image
- download (to dev)
- share
- new - Detailview preview - pdf viewer: "Fit to screen size" vs. "Fit to screen width"
- share (new logic - first version, basic)
- Section 81
- share (new logic-complex)
- Teilen
- Share - version 1 (to dev) - not availble
- Share - version 1 (new)
- Share - other tabs
- Add modul header in shared document page
- **File navigation problem**
- Attaching new files in the existed Dokument

### 2. Add new 页（node-id 417-47532）—— 上传新文件
Add new 页（上传新文件）——对应 Hochladen von Dokumenten。能看出上传流程里有：拖拽区、超大文件处理（Oversized files）、可关闭的通知横幅、两个快捷操作（"只上传已完成的文件" / "移除这些文件"）、上传弹窗（Add new - popup），以及 Indexing 和 Email with attachment 两个我不确定具体含义的模块。

- Add new（×2）
- Frame 2610315
- Frame 2610369
- short cut 2 - upload only the completed files
- short cut 1 - remove these files
- Add new - new mockups
- Section 2
- dismissible notification banner
- Oversized files
- Frame 2610380
- Frame 2610381
- Add new - popup
- Indexing
- Attaching new files in the existed Dokument
- Email with attachment

### 3. Folder 页（node-id 2168-10801）—— 文件夹（文件列表左侧区域上半部分，structure tab 旁边的 tab）
Folder 页——对应 Ordnerstruktur anlegen/verändern。能看出：文件夹网格布局新版、下拉菜单里的文件夹树、空文件夹状态、文件夹路径（面包屑）、拖拽操作，以及出现两次的 Workflow。

- Folder（×2）
- Section 1
- Section 2
- Section 3
- Workflow（×2）
- Drag and drop
- Folder - new layout in grid
- Folder tree in dropdown
- Folder without document
- Folder path
- drag drop
- Minimal width of filter area

---

## 第二部分：brainstorming 梳理

### 本次进度概览

1. 用户给出已上线的7大主要功能区块清单，并提供三个 Figma 页面链接，要求梳理各页面 section 结构。
2. 改用 Figma MCP 的 `get_metadata` 直接读取（比此前会话里靠浏览器截图准确得多），一次性拿到三个页面的完整 section 列表。
3. 从 section 命名初步判断三个页面分别对应哪块已上线功能，标记出4个含义不清楚的 section，逐一向用户确认。
4. 用户逐条澄清，同时点出一个关键信号：**设计细节量非常大，作品集不可能全部展示，后续需要我帮忙从中筛选出值得讲的重点**——这是一个角色定位的转折点，记录下来。

### 已上线7大功能 ↔ 三个 Figma 页面的对应关系

- **Hochladen von Dokumenten**（上传文档）→ 对应 **Add new 页**：拖拽上传区、超大文件处理、可关闭通知横幅、快捷操作（只上传已完成的文件/移除文件）、上传弹窗、Indexing（元数据批量套用）、邮件类文件的展示优化。
- **Herunterladen von Dokumenten**（下载文档）+ **Teilen von Dokumenten**（分享文档）→ 主要对应 **Detailview 页**里的 `share & download`、`download (to dev)`、以及经过多轮版本迭代最终定名为 `Teilen` 的分享功能。
- **Dokumenten Viewer**（文档查看器）→ 对应 **Detailview 页**的核心内容：全屏预览、PDF viewer 的两种适配模式（Fit to screen size / Fit to screen width）、ZIP 和 360 Image 的预览能力、文件间导航（含 File navigation problem 这类交互细节打磨）。
- **Ordnerstruktur anlegen/verändern**（创建/修改文件夹结构）→ 对应 **Folder 页**：文件夹网格布局新版、下拉菜单文件夹树、空文件夹状态、文件夹路径（面包屑）、拖拽操作。
- **Dashboard** → 对应独立的 **Dashboard 页/canvas**（详见下方"第三部分"），已有素材。
- **weitere Grundfunktionen des Projektraums**（项目空间其他基础功能）→ 仍待补充素材（用户提供的一批候选链接核实后确认实际属于 Dokumenten Viewer，见下方说明）。

### 4个澄清问题的回答整理
我的回答：
· Add new 页里的 "Indexing" 具体指什么功能？ → indexing指的是为该文件在文字框里填写的内容，如日期、文件类型、公司等，这个功能是指把已填写的内容做一个整体应用与其他上传的文件
· "Email with attachment" 是否是已经上线的独立上传渠道？ → 不是邮件上传渠道，是当上传文件中有带附件的邮件时，如何显示。这是个后补充的内容，不是之前不能上传邮件，而是没有考虑展示附件，现在想把邮件中的附件也展示出来并提供删除功能。
· Detailview 页里的 "ZIP" 和 "360 Image" 具体是什么能力？ → viewer预览能力
· "File navigation problem" 具体是什么问题？ → 是一个测试出来的很细节的问题，意思是在文件预览的下方有个写着文件名字的地方，它的左右两侧有导航箭头，相当于导航到下一个、上一个文件的按钮，这个按钮原设计是根据文件名字的长度而变化，只设定了一个最大宽度，导致右侧箭头的位置会移动从而为点击下一个造成难度。这个说的太细节了，只是让你有个印象，具体的设计会有非常多的细节考量、修改等，肯定是无法完全展示出来。就得你帮我筛选作品集里的展示内容、重点。

1. **Indexing**（Add new 页）：不是自动识别/自动分类，而是把某个文件已经填好的元数据字段（日期、文件类型、公司等）**批量套用到其他上传文件**上，本质是一个减少重复填写的效率功能。
2. **Email with attachment**（Add new 页）：不是新的上传渠道（本来就支持上传邮件文件），而是后补的**展示逻辑**——当上传的文件本身是"带附件的邮件"时，把邮件正文和附件拆开展示，并支持单独删除附件。
3. **ZIP / 360 Image**（Detailview 页）：都属于 **Dokumenten Viewer 的预览能力**，能直接在viewer里预览压缩包内容和360度全景图片，不是打包下载功能。
4. **File navigation problem**（Detailview 页）：一个很细节的交互问题——文件预览下方有文件名+左右导航箭头（跳转上一个/下一个文件），原设计箭头位置随文件名长度变化、只设了最大宽度，导致右箭头位置漂移，增加了点击难度。

### 关键 meta 信号（值得记录）

用户明确表示：类似"导航箭头漂移"这种细节问题非常多，逐条讲会让作品集失焦，**需要我帮忙从大量设计决策细节里筛选出真正值得放进作品集的重点**。这标志着接下来的协作重心会从"梳理素材、搞清楚做了什么"逐步过渡到"提炼叙事、决定讲什么"。

## 第三部分：Dashboard 素材 + Dokumenten Viewer 补充细节（新一轮问答）

用 `get_metadata` 分别读取用户提供的 5 个 node（均属 `PR-Mockups` 文件，fileKey `tjxt0kdhKrFib5jyt8Ai5N`），只取直接子层级（避免图标/矢量细节把结果撑爆）：

### Dashboard（node-id 977-17475，独立的 canvas）
34 个顶层元素，主要包括：
- **`Startansicht (Dashboard)`**——主仪表盘视图（25 个子节点）
- 一批 widget 文本标签：`Firmen`（公司）、`Strukturen`（结构，×2）、`Aufgaben`（任务）、`Mängel`（缺陷）、`Dokument Typ`（文档类型）、"10 documente hochgeladen / 10 editiert" 这类计数文案——和 `1.brainstorming.md` 里描述的"文档总数、按用户/类型分布、可按类型/公司筛选"吻合，另外多了 Mängel（缺陷）相关内容，之前没提到
- `Widgets`、`Filter und Chips`、`Line chart`、`Live layout suggestions`（实时布局建议）、`Dokumente navigieren in Kachel 'Zuletzt hochgeladene Dokumente'`（在"最近上传文档"卡片里导航文档）等具名 section
- 若干未命名的 `Section 69/71/73/74/89/90`，推测是探索性/迭代版本

### weitere Grundfunktionen des Projektraums 的归类澄清
用户最初提供了 4 个链接（Fullscreen、System info、Attaching new files + filter、min/max width）想归入这一类，**但确认后这 4 个实际都属于已有的 Dokumenten Viewer 分类**，不算 weitere Grundfunktionen 的素材。也就是说 **weitere Grundfunktionen des Projektraums 目前仍没有对应的 Figma 素材**，需要用户后续再补充。

这 4 个链接本身给 Dokumenten Viewer 补了不少细节，记录如下（都在 Detailview 页 `542-59643` 下面，是之前 section 列表里已经列过的项）：
- **Fullscreen**（690-190236）：全屏预览模式的多个版本变体框架（`7024456`、`2`、`7024457`、`Variant 2-2` ×2、`Preview + Attributs (more documents)`）+ `Popup`/`Detail view` 标签。
- **System info**（542-59646，即 `Detailview - System info`）：不可编辑的系统元数据 tab——文档 ID/编号、来源、上传/创建/最后修改的时间和人、文档版本；每个文件还有文件名/日期/上传时间/上传人/大小/版本/文件 ID，按添加顺序排序。另有多个 `FILTER option 2.` 版本变体框架，以及行高 32/40 的行样式探索（`行高32`/`行高40`）。
- **Attaching new files in the existed Dokument + filter**（2580-46525，追问后补了 filter 具体 node：2580-46526）：5 个 `Toolbar of detail view layout` 版本变体，呼应此前"多版本讨论、多轮测试"的迭代过程记录。深入 2580-46526 这个版本后看到完整三栏布局（左侧文件夹树 `tree` + 顶部 `Chip & Search Bar (desktop) - filter chips`，即用户说的"过滤器"——一组按 `Records` 分类的筛选 chip 组件，位于文档列表区顶部；中间文档网格；右侧 Detail view 面板，含 `Preview / Basic / Dokumentklasse / System infos & Files / Teilen / Kommentar / Historie` 多个 tab（部分隐藏，说明也是多方案探索中的一版）），和 `Document grid`（`PR - to dev` 文件）里的最终定稿设计结构非常接近。
- **min / max width**（895-16917）：各面板最小宽度规格标注——Records/文档列表区（422px 或 260px）、Detail view（400px，默认占工作区 1/3 宽度；或 280px，默认 1/3）、Filter area（默认占工作区 1/4 宽度）、File viewer（560px / 300px / 560px，多个版本）。是一份响应式布局的宽度约束规范。

### 待补充 / 尚未确认（已更新——见下方最新一轮问答）

- ~~**Workflow**（Folder 页，出现两次）：具体指什么工作流，未展开。~~ **已确认**：是"根据文件夹创建和上传文件至文件夹"的流程，用 FigJam mockups 具体展示，两个板子：
  - https://www.figma.com/board/rfqKIETIYgAS06fDL8Sxx7/PR---Folder-Workflow?node-id=2-43
  - https://www.figma.com/board/rfqKIETIYgAS06fDL8Sxx7/PR---Folder-Workflow?node-id=2-13
- ~~**share 相关多个版本**……哪个是最终上线定稿版本，未确认。~~ **已确认**：**Share - version 2** 是最终定稿版本，在 `PR - to dev` 文件（非 `PR-Mockups`）：
  - https://www.figma.com/design/QijNgSwjUjvCbhWcUnwx9f/PR---to-dev?node-id=1654-18351
  - 其余版本（`new logic - basic/complex`、`Share - version 1 (to dev)/(new)`、`Share - other tabs`，以及 Detailview 页里叫 `Teilen` 的那个）都是迭代过程中的历史版本，不是最终定稿。
- **annotation**（`Share/save/annotation` 里提到）：用户表示不重要，跳过确认，标记为**待定/不纳入作品集讨论范围**。
- ~~**Folder tree in dropdown**：是否在其他场景被复用为同一个组件。~~ **已确认**：是，在多个场景（如上传时选择目标文件夹等）复用同一个组件。
- ~~**weitere Grundfunktionen des Projektraums** 和 **Dashboard** 两块已上线功能，目前还没有对应的 Figma 页面素材。~~ **部分确认**：Dashboard 已有素材（见上）；weitere Grundfunktionen 仍待补充（见上方归类澄清）。

### 下一步

本轮已确认：Workflow、share 定稿版本、Folder tree 复用、Dashboard 素材。annotation 已跳过。**唯一仍待补充的是 weitere Grundfunktionen des Projektraums 的对应素材**——其余 6 个已上线功能区块都已有对应 Figma 素材。是否现在补这最后一块，还是直接开始"从细节里筛选作品集重点"这一步，留给用户下一轮决定。
