# 作品集筛选记录（续）—— 2026-08-01

> 承接 `brainstorming.md` 里已确定的方向和筛选框架，本文档记录 2026-08-01 这次对话的内容：把候选项目按四个类别粗略列完，并开始整理打分所需信息。

## 本次进度概览

按 `brainstorming.md` 里定好的计划，依次过完了四类 B2B 候选（汽车 HMI 差异化案例用户已在此前单独完成，本轮不涉及）：

1. 0→1 新产品/模块 → 4个候选
2. 复杂工作流表单系统 → 2个候选
3. 数据仪表盘可视化 → 2个候选
4. 设计系统/组件库 → 1个候选（默认入选）

素材清单已列完，下一步是补充"影响力可证明"数据后用框架打分，选出各类别代表案例。

---

## 第一类：0→1 新产品/模块（4选1，尚未打分）

1. **文件管理软件（PR）**——不是通用文件管理工具，是针对基建/建筑工程项目管理的工作方式设计的，与公司其他产品互联。
2. **会议纪要软件（JF）**——同样不是通用会议纪要工具，也是围绕基建/建筑工程项目管理场景设计，并与公司其他产品打通。
3. **BIM 模型缺陷创建板块**——在已有产品上新增的板块：可在 BIM 模型上选中局部区域来创建缺陷记录。属于"老产品里的新模块"，不是全新产品。
4. **桌面端软件的移动端产品**——为原本桌面端的软件重新设计/制作对应的移动端产品。

**重要更正**：最初以为"贴合基建/建筑工程场景 + 生态互联"是1、2号项目的差异化特点，用户澄清这其实是**全部项目的共同底色**，不能用来区分具体案例——它是整个作品集的品牌调性/大前提。真正决定每个案例故事角度的，要落到更具体的层面：各自解决的具体工作流问题、用户角色、设计决策难点分别是什么。

---

## 第二类：复杂工作流表单系统（2选1，均无 NDA）

1. ~~缺陷创建时快速加附件~~——创建缺陷时可快速拍照/添加图片/添加文件/添加平面图截图。**已归类**为第一类第3项（BIM 缺陷创建板块）案例里的细节佐证，不单列为候选。补充信息：这其实是一次产品功能迭代——基于用户反馈，在多个版本里讨论、经过多轮测试后才最终确立，为用户节省了大量任务时间。"发现问题→多版本讨论→多轮测试→最终方案"的迭代过程故事完整，适合作为 BIM 缺陷案例里"设计过程"的具体佐证素材。

2. **GU/NU 验收流程**——总承包商（GU）先提供数据；分包商（NU）与 GU 共同编写验收报告，NU 先签署；进入等待期，直至 GU 启动接收（Übernahme）；最终 GU 完成全部确认并签署，流程结束。真正符合"复杂工作流"定义：多角色（GU/NU）、顺序签署、等待态、状态流转清晰。**无 NDA**，可展示真实界面。

3. **移动端质量检验记录（Prüfprotokoll）采集功能**——建筑工地移动端，施工人员按预设检验模板，逐个标段、逐条检验在手机上记录检验结果（或继续填写/更改）。流程：从检验模板 → 选择已填写的主表单 → 进入主表单或选择其子表单 → 子表单逐级下钻填写检验结果，可在主表单与子表单之间来回跳转、逐项保存/发送，直至全部完成。特点：多层级下钻导航（模板→主表单→子表单）+ 移动端小屏幕上处理复杂层级信息架构。**无 NDA**，可展示真实界面。

**与 GU/NU 的差异**：GU/NU 是多角色顺序流程，移动端检验是单人多层级信息架构 + 移动端约束——两者复杂度类型不同，叙事互补性不错，不算同质化。

---

## 第三类：数据仪表盘可视化（2选1，均无 NDA）

1. **文件管理软件（PR）的 Dashboard**
   - **展示内容**：建筑项目文档库整体活动概览——文档总数、近30天新增/查看趋势、按用户和文档类型的分布、最近上传明细；可按时间、人员、类型、公司筛选。
   - **给谁看**：项目经理、文档管理员，其次是管理层。不给现场人员看。
   - **解决的问题**：把"文档流转是否健康、各参与方是否按时提交和查阅资料"从主观感觉变成可量化、可追溯的数据，用于过程管控和事后举证。

2. **缺陷管理软件的 Dashboard**（对应第一类第3项 BIM 缺陷板块所在的产品）
   - **展示内容**：跨全部20个项目的组合级（portfolio）概览——项目数量分布、各项目数据集完成率、不同 layout（如门窗缺陷、管道缺陷等各类缺陷管理表对应的项）的缺陷数量。
   - **给谁看**：管理层、项目组合负责人。不是给单个项目现场人员看的——视角是横向跨项目对比，不深入单个项目内部。
   - **解决的问题**：一眼定位"哪个项目、哪类缺陷（layout）的完成度"如何。

**两者互补性**：第一个是单项目内、按时间趋势看"文档流转健康度"；第二个是跨20个项目的组合级、按类别看"完成率对比"。一纵一横，不算同质化。

---

## 第四类：设计系统/组件库（仅1个候选，默认入选，无 NDA）

**公司设计系统（从0搭建）**
- 几乎从0开始建立设计系统，现在是产品经理和开发人员的重要依据——被查看、对照、用于测试。
- **建立了 design token**，目前处于测试阶段。
- **组件库**：正在尝试用 AI 辅助，1:1 还原设计系统里的组件（进行中的新尝试，可作为"用新工具解决还原效率/一致性问题"的亮点）。
- **搭建前的问题**：多个产品视觉不统一、PM和开发对齐成本高、没有统一规范导致一系列具体问题（待深挖阶段展开成具体例子/数据）。

---

## 候选汇总

- **0→1 新产品/模块**（4选1）：①PR文件管理　②JF会议纪要　③BIM模型缺陷创建板块　④桌面转移动端产品
- **复杂工作流表单系统**（2选1）：①GU/NU验收流程　②移动端质检记录Prüfprotokoll下钻采集
- **数据仪表盘可视化**（2选1）：①PR文档Dashboard　②缺陷管理Dashboard
- **设计系统/组件库**（仅1个，默认入选）：公司设计系统从0搭建

汽车 HMI 差异化案例：用户已在此前单独完成，不纳入本轮讨论。

## 下一步

用筛选框架（`brainstorming.md` 里的5条标准）给以上候选打分，重点是补上"影响力可证明"这条——目前素材里大部分还缺数据或明确的定性反馈。计划从候选最多的第一类（0→1新产品）开始逐个补充效果数据。
