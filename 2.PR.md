# 作品集筛选记录（续二）—— 2026-08-03

> 承接 `brainstorming.md`（筛选框架确定）和 `brainstorming2.md`（候选素材清单列完）。本次对话内容：为第一类候选"文件管理软件（PR）"补充"影响力可证明"数据，过程中连接了 Figma MCP、用浏览器读取了三份相关 Figma 文件，并记录了工具链排查过程。
---

## PR 项目 Figma 素材内容（本次读到的）

用户提供的三个链接，账号选择用 `wdfan26@gmail.com`（Editor 权限）打开：

1. **Functional Map**（`PR - map & flow` 文件，node-id 7-5350）
   - 按角色分泳道的功能地图，角色包括：Projektleiter/PL（项目经理）、Bauleiter/BL（工地负责人）、Architekt/Planer/Fachplaner（建筑师/规划师）、Sekretariat/Assistenz（项目行政）、Subunternehmer/Nachunternehmer（分包商）、Polier（工头）。
   - 每个角色对应不同的文档操作：搜索/分发文档、审核文档（Document preview）、创建计划并上传（Pläne erstellen/hochladen）、批量编辑（Sammelbearbeitung，ZIP、自动填充）、审核新文档版本、接收任务、上传/分发文档、打开计划等。
   - 左侧有一份"Shared data"图例，列出十几类颜色编码的功能类别（如自动化通知配置、快速查文档、多文件上传批量编辑、聊天沟通等）。
   - 右侧有一列按角色区分的编辑/删除/查看权限图标，说明这是一个**权限分层、多角色协作**的系统。
   - 旁边还有 "Personas" 区块（未细看）。

2. **User Story Map**（同文件，node-id 7-4305）
   - 结构为 Stage（顶层活动）→ Details（用户故事卡片），按 "Volumen 1 / 2 / 3"（分批次/阶段）横向划分。
   - 说明产品是**分阶段迭代交付**的，不是一次性大版本上线。

3. **Workflow / Detailview**（`PR-Mockups` 文件，node-id 542-59643）
   - 页面结构：`overview, feedbacks` 页 + `Detailview` 页。
   - **`overview, feedbacks` 页**里有一封 **2026年7月的真实客户邮件**（发件人 Wolfgang Schmid，收件人客户 Herr Ripperda）：
     - 客户主动写信要求提前使用（"vorzeitige Nutzung"）还未正式发布的 **"Projektraum-Modul"**（项目空间模块，产品名 **mydocma**）。
     - 公司为此专门在演示环境开放了 Beta 版本供其测试，测试范围包括：文档上传/下载/分享、文档查看器、Dashboard、文件夹结构创建/修改。
     - 邮件说明这是 Beta 版本，部分功能（如下拉字段自定义）尚未完全可用；计划到当年12月底前逐步补齐 AI 搜索/聊天机器人、跨模块自动归档、权限角色系统、多字段自定义、多重并行文件夹结构、计划管理、项目参与方名单等功能。
     - **信号**：客户主动要求抢先体验未发布功能，是一个较强的定性"影响力"佐证——说明这个模块被目标客户视为有明确价值和迫切需求的东西。但目前**还不是效率提升数据或满意度评分**，只是"客户主动要求"这个事实。
   - **`Detailview` 页**里有具体功能设计的图层，其中"Attaching new files in the existed Dokument"（向已有文档追加新文件）这一块有多版 "Toolbar of detail view layout" 方案对比：
     - 每版结构大致是三栏布局：左侧深色图标导航栏、中间文档列表（表格，含"名称+日期"一类字段）、右侧详情/预览面板（缩略图预览 + 可编辑元数据字段 + 类似评论/协作留言的小图标 + 底部红色"löschen"删除入口）。
     - 各版本的差异主要在**顶部工具栏的图标排布**，看起来是在测试"追加文件"这个动作放在哪、用什么图标最不容易和"新建文档"混淆。
     - 与 `brainstorming.md` 里提到的"多版本讨论、多轮测试才最终确立"的迭代过程吻合。
     - 另有 "File navigation problem" 图层（命名暗示是具体问题记录），本次未展开细看。
     - 附近还有 "ZIP"、"360 Image" 等功能的 mockup，本次未展开。
   - 部分字段文字因画布分辨率限制未能读清，需要用户后续补充确认。

4. **Document grid**（`PR - to dev` 文件，node-id 709-3159）
   - **页面定位**：这是产品的实际"待开发"（to dev）设计稿，即文档管理模块的完整落地设计，可与上面 `Workflow / Detailview` 里的早期 toolbar 迭代方案对照，体现"多版本方案讨论 → 最终定稿设计"的完整过程。
   - **布局结构**：三栏式界面——
     - 顶部导航栏：项目切换（Projekt 1 下拉）、版本号、用户头像。
     - 左侧：文件夹树状结构（Struktur tab，多级可折叠：Abschnitt → House → Floor）+ 筛选面板（Dateidatum/Herkunft/Dokumenttyp/Tags 等多维度筛选下拉）。
     - 中间：文档数据表格（列：File type / Document title / Document type / Document date / Origin 等），支持筛选 chip（如 "A.1F - First Floor ×"）、新建/搜索/刷新操作。
     - 右侧：详情预览面板，含 Preview / Basic / Dokumentklasse / System infos & Files / Teilen 多个 tab；缩略图列表 + 大图预览，支持缩放（100%）、上一张/下一张切换，底部显示文件名/类型/日期/大小。
   - **与已有素材的关联点**：
     - 呼应 `brainstorming.md`/`brainstorming2.md` 里"复杂度和角色"这条筛选标准——树状文件夹结构（Abschnitt/House/Floor 多级）体现了对建筑工程项目层级结构的贴合设计。
     - 与 `Workflow/Detailview` 页的 "Toolbar of detail view layout" 多版本方案是同一功能区域（详情预览面板）的不同阶段设计，可作为"设计过程"故事的具体证据链（早期方案对比 → 最终稿）。
   - **待补充**：具体筛选字段、树状结构的层级设计逻辑（为什么按 Abschnitt→House→Floor 分级）、详情面板 tab 划分背后的信息架构决策——这些"为什么这么设计"的决策逻辑目前只能从截图推测，需要后续补充实际设计过程/决策背景，才能满足"故事完整度"标准（问题→决策过程→为什么→结果）。


另外两个文件

我读完了这份文档。 Basic info  User stories 这份材料价值很高，比目前案例里用到的信息细得多，尤其是这两块：

1. Meeting minutes（会议纪要，2025-05-15 / 05-22 / 06-12，带日期和参会人缩写）
这是最值钱的部分——真实的、逐次演进的产品决策记录，比如：
- 权限设计：用 Docuware 风格的"过滤器绑定角色权限，多个用 OR 连接"来做，而不是传统的逐文档授权
- Tags vs PX Tags 的概念澄清：两者是完全不同的东西（PX Tags 是既有的标签树，Tags 是新引入的扁平标签概念），这个"术语歧义"在 5/15 被明确解决
- Delete 策略的权衡：v1 不做真删除，只做标记删除，且讨论了"回收站"导航入口
- 新增文档弹窗的具体设计取舍：左预览右输入、三个 tab（基础信息/文档类输入/系统信息）

这些是带时间戳、带决策过程的真实证据，正好能填补我们之前讨论的"方法四·决策日志型"或强化 System design 部分说服力——现在案例里的"7 个已上线功能"是结果罗列，而这份文档能让读者看到"权限该怎么设计""要不要做真删除"这些判断是怎么做出来的。

2. Out of Scope（v1 范围之外）清单
明确列出了主动砍掉的功能（真删除、文档版本、评论、保存筛选条件等），这是"做减法"判断力的直接证据，目前案例里完全没有体现这一层。

3. Problem Statement / Product Vision Statement 原文
比现在 Background 部分的转述更精炼，可以考虑直接引用或对照润色。

你提到"另外两个文件"，但这条消息里只给了一个路径——麻烦把第二个文件路径也发给我，我一起读完再给你一个综合建议（哪些内容放进哪个 section）。



Basic info & User stories 和 Personas 两份文件 分析：

两份材料放在一起看，价值定位很清楚：第一份（Basic info & User stories）是"决策证据"，这份 Personas 是"角色证据"——刚好分别能喂给我们之前讨论的两个强化方向。

这份 Personas 文档比案例现有内容详细得多：

现在案例里的角色地图只列了 5 个角色（PL、Bauleiter、Architekt、Subunternehmer、Sekretariat），且是抽象的架构图附属信息。这份文档有 8 个角色，每个都带"需求-挑战-目标-常用文档类型-办公场景"五个维度，多出的 3 个角色其实很有戏剧性：

- Bauherr/Auftraggeber（业主/发包方）——外部用户，只能看部分文档，需要"技术小白也能看懂的清晰概览"，这是一个"对内 vs 对外"权限设计的具体例证
- Polier（工长）——现场执行团队的头，和 Bauleiter 区分开，同样是 Baustelle/Tablet 场景，强调"操作要足够简单，不是每个工长都熟悉复杂软件"
- Behördenvertreter/Gutachter（监管/验收方）——外部审查人员，核心诉求是"所有变更必须可追溯，以保证验收报告的准确性"——这是一个合规/审计角度的持牌人，之前案例完全没提到

几个可以直接强化案例的具体点：

1. 设备/场景矩阵是现成的证据：PL/Architekt/Bauherr/Sekretariat/Behördenvertreter 是 Büro+Desktop，BL/Polier 是 Baustelle+Tablet，Subunternehmer 是两者都有。这直接支撑现在 System design 部分"办公室全局视图 vs 现场 30 秒查询"这个设计判断——但目前案例只讲了两端（PL vs 现场工长），这份文档能让你把"为什么要分设备场景做设计"讲得更立体，而不只是二元对立。
2. 权限设计的角色依据：Subunternehmer"只能看自己公司的文档"、Bauherr"只能看部分文档"——这两条直接对应上一份文档里"权限用过滤器绑定角色"的技术决策，两份材料能拼成一条完整的因果链：角色需求→权限设计决策→功能实现。这条链正好是"决策日志型"最需要的素材。
3. Behördenvertreter 的审计追溯需求，如果案例里有类似"变更记录/版本追溯"的功能，这是一个很有说服力的"外部合规压力驱动设计"的例子，能让 Systems-thinking

建议怎么用（不涉及立即改文件，先跟你对齐方向）：

- 若采纳我们之前讨论的"方法六角色视角"强化：这份 Personas 文档基本就是那一节的素材来源，可以直接用（尤其是 Bauherr 和 Behördenvertreter
- 即使不做整节角色视角，也建议把 Background 部分"8 类干系人调研"这句话背后的角色多样性做实——现在案例里这句话没有具体展开，这两份文档能让它从"我们调研了 8 类干系人"变成有血有肉的例子。
- 第一份文档里的 meeting minutes 决策证据，建议放进 System design 或新增一个"关键决策 Delete这几个真实取舍。


---

## 待补充（下一步）

针对"文件管理软件（PR）"这一候选，还缺：

1. **量化效果数据**：Beta 测试之后有没有正式上线？上线后的采用情况、效率提升数据（比如文档处理/查找时间缩短了多少）、或客户满意度反馈？  （已补充，见1.数据收集方法）
2. **"追加文件"功能的具体问题背景**：最初的痛点是什么（操作路径太深？容易误操作？）？多轮迭代后最终定稿方案相比最早版本改了什么？
3. **"File navigation problem" 具体内容**：这是什么问题，怎么解决的？

打分框架五条标准（见 `brainstorming.md`）里，目前 PR 项目已经比较扎实的是"复杂度和角色"（多角色权限系统）和"故事完整度"的设计过程部分（toolbar 多轮迭代）；"影响力可证明"这条目前只有定性信号（客户主动求早鸟），量化数据待补。

补完 PR 项目后，按原计划继续过第一类剩下候选（JF会议纪要、BIM缺陷创建板块、桌面转移动端）。
