# PR 案例文案初稿

> 承接 `6.PR案例文案大纲.md`（六板块结构，已全部确认）+ `7.PR案例配图清单.md`（配图核实结果）。本文档把大纲扩写成可以直接使用的正式文案，第一人称口吻，风格对齐现有站点（`components/Hero.tsx` "你好，我是设计师"）。**板块4（深度案例主线）本轮先给出结构骨架，具体内容和配图下次再展开**——因为板块4还有三张关键测试截图的来源没确认（见 doc7），先把其余五个板块的文案定下来。

配图标注方式：每个板块下用 `[配图：xxx]` 标出建议插入的图片，对应 `7.PR案例配图清单.md` 里的核实结果。

---

## 板块 1｜Hero

**一句话定位**

> Project Room（PR）—— mydocma 生态里为建筑工程项目打造的集中式协作与文档管理平台。

**副标题/引导句**（呼应站点 Hero 的语气）

> 建筑工程项目的文档天然分散在七八个专业系统里。PR 不是又一个文件夹，而是把它们汇聚到一处的接收端。

**关键信息条**
- 角色：与 Elisa 共同负责设计，体现跨职能协作
- 交付节奏：分阶段迭代（User Story Map Volumen 1/2/3）
- 平台：Desktop + Tablet（办公室与工地两种使用场景）

`[配图：Hero 主图 —— Document grid 三栏界面，PR - to dev 文件 node 709-3159]`

---

## 板块 2｜背景与挑战

建筑工程项目的文档，从来不是"存在一个地方"这么简单。

现场检查用 AppBuilder，日报用 RP，缺陷记录用 MM，会议纪要用 JF，图纸用 PX —— 同一个项目的信息，散落在七八个专业系统里，没有一个地方能看到全貌。这不是某个团队疏于管理，而是建筑工程行业的工作方式决定的：不同专业角色本来就活在不同的工具里。

我们梳理了 8 类项目干系人的使用场景后发现，几乎每一类角色都把"文档分散、找不到、格式不统一"列为核心痛点，其中项目负责人（Projektleiter）明确把"数据碎片化"（Datenfragmentierung）标记为首要挑战。

PR 要解决的不是"再建一个文档库"，而是做**接收端**——通过 PR-API / Files-API 主动对接各专业模块，把文档自动同步进来。用户不需要在多个系统之间来回切换，PR 已经把该有的信息聚在了一起。

这个方向是不是选对了，2026 年 7 月一封客户主动发来的早鸟申请邮件（Wolfgang Schmid 写给 Herr Ripperda，希望提前用上 PR）给出了一个真实的信号。

`[配图：自绘示意图 —— "7个专业系统 → 1个 PR" 的信息聚合示意，用代码/SVG 实现，不需要额外 Figma 素材]`

---

## 板块 3｜系统设计概览

PR 要同时服务的，是一群工作方式完全不同的人。

坐在办公室里的项目负责人需要看全局、管权限、追版本；戴着安全帽在工地上的工长，可能只有几十秒时间，用平板打开一张图纸核对现场情况。同一份文档，要在两种完全不同的使用场景里都"好用"——这是 PR 系统设计上最核心的约束。

下面这张图是我们梳理的角色协作流程：项目负责人（PL）、现场负责人（Bauleiter）、建筑师/规划师（Architekt）、分包商（Subunternehmer）、行政助理（Sekretariat）等角色，各自在什么节点介入、文档如何从一个角色流向下一个角色。

`[配图：Functional Map，FigJam fileKey aQkJDd84KYn9Wi1MZNE1eU，node 7:5350]`

**已确认（2026-08-15）**：板块3新增 User Story Map 配图，紧跟在 Functional Map 后面。

理清协作流程之后，我们把整个模块要做的事情用 User Story Map 拆解成 Activities → Steps → Details 三层，再按 P0/P1/P2 分成三个 Release 逐步交付——这也是下面"分阶段交付"这个判断背后具体的规划方式。

`[配图：User Story Map，用户直接提供截图（非 Figma 导出），已存为 public/images/pr-user-story-map.png]`

围绕这个协作模型，PR 的系统设计还有两个值得一提的判断：

- **两种组织方式并存**：Structure（从项目管理模块同步过来的技术层级，系统生成、用户不可改）和 Folder（用户自定义的灵活文件夹树）并行存在，分别满足"系统性归档"和"个人习惯整理"两种需求，不强迫用户二选一。
- **分阶段交付**：整个模块按 User Story Map 拆成 Volumen 1/2/3 逐步上线，不是一次性设计到位再交付。

**已上线的核心功能**（概览，不逐一深挖）：

| 功能 | 说明 |
|---|---|
| 上传文档 Upload | 拖拽上传、批量处理、超大文件提示 |
| 下载文档 Download | 单/多文档下载 |
| 分享文档 Share | 生成受限链接，与外部伙伴协作 |
| 文档查看器 Viewer | 支持 PDF、图片、ZIP、360 全景等多种预览 |
| Dashboard | 项目文档总览、按类型/公司筛选统计 |
| 文件夹结构 Folder | 自定义文件夹树，支持拖拽整理 |
| 跨模块集成 | 与 AppBuilder、PX 等模块打通，文档自动同步 |

跨模块集成不是一句"做了 API 对接"就能说清楚的。比如 AppBuilder 里现场检查添加的照片同步到 PR 时，"谁上传的""谁创建的"这两个字段在跨模块场景下要怎么定义都需要单独澄清——细节多到超出这个板块要展开的范围，只在这里点一句。

`[配图：Document grid 或 Dashboard 缩略图，作为功能概览的视觉支撑，可选]`

---

## 板块 4｜深度案例主线 —— Upload 可用性测试迭代

~~本板块下次具体填内容，这里先占位结构，方便先把其余板块定稿。~~
**已确认（2026-08-15）**：素材和文案已全部补全，来源是用户直接提供的两份可用性测试文档截图（`PR Usability test_测试原话或反馈.jpg`、`PR Usability test_ 具体问题描述.jpg`，4 位参与者 Robert/Ivan/Max/Dan，两轮测试：pr-test 轮测现有产品、prototype 轮测新原型）+ 新版 mockup 流程图（`Add new - new mockups.png`）+ 一段 73 秒操作录屏（`upload document.mov`）。

**引言**：PR 上线后，我们没有假设"新建/上传文档"这条最高频的操作就一定好用——而是做了两轮可用性测试，每轮找 4 位参与者，先测线上版本，再测重新设计的原型。

**问题**（第一轮，测线上版本）：
- 2/4 参与者在点击"ablegen"（归档）之后，发现弹窗还留在原地，不确定文档到底传没传上去——原话："以为这三个文档已经上传完了。"
- Inbox 入口没人真正看懂：有人猜"跟 Docuware 差不多"，有人以为是用来收发邮件的，还有一位直接跳过没管（该参与者也没用过 Docuware）。
- 属性表单里的"Tags"字段让人卡壳——"Tags 是什么？"

**过程**：重新设计"新建/上传文档"流程——原来含糊的"点了就传"，改成明确的三选一入口（存进 Inbox / 合并成一个文档 / 每个文件单独建一个文档），属性表单加上实时校验，必填项没填立刻标红。带着新原型做了第二轮测试。

`[配图：新流程逻辑图 Workflow-NEW，用户直接提供，已存为 public/images/pr-upload-workflow-reference.png]`
`[配图：三选一入口 + 实时校验警告，裁自 Add new - new mockups.png，已存为 public/images/pr-upload-choice.png、pr-upload-validation-warning.png]`

**决策**：
- "合并成一个文档"选项，命中了 3/4 参与者"想要一次性把所有文件存起来"的诉求
- 实时校验反馈解决了"点了上传才发现失败"的问题——必填项漏填立刻标红

`[配图：pr-upload-all-in-one.png、pr-upload-validation-resolved.png]`

**结果**：第二轮测试，4/4 参与者顺利走完了整个新建文档流程——"结果出乎他意料地顺利"。诚实呈现新问题：想在"属性填写"这一步给已选文档追加文件时，4/4 参与者都卡住了，更希望有个"+"按钮——这个改动还没做，留到下一轮迭代。

`[配图：pr-upload-success-grid.png]` `[视频：pr-upload-demo.mp4，实际操作录屏，73 秒，已用 ffmpeg 转封装为 faststart mp4]`

---

## 板块 5｜系统性思维佐证

**已确认（2026-08-17）**：两张配图已从 Figma node `895-16917`（PR-Mockups）裁出（默认三栏布局一张 + 打开 File viewer 时一张，裁成两张避免整图缩小后看不清），另一张 Structure 树状结构直接从板块1 Hero 图裁出特写（未使用 Folder 页 node 2168-10801——该节点导出下来是 119928×12624px 的整页横向画板，不是单独组件，裁 Hero 图更干净）。已存为 `public/images/pr-panel-widths-default.png`、`pr-panel-widths-fileviewer.png`、`pr-structure-tree.png`。已落地到代码。

好的界面设计，不只是把元素摆对位置，还要经得起"各种屏幕配置组合起来还合理吗"这种追问。

PR 的工作区可以同时打开文件夹树、文档表格、筛选面板、文件预览——面板数量和组合是可变的。为此我们为每个面板都定义了最小宽度和默认占比：文档列表区（Records area）最小 422px，详情面板（Detail view）最小 400px、默认占工作区三分之一，筛选面板（Filter area）默认占四分之一，文件预览（File viewer）最小 560px。不是"看起来差不多就行"，而是有明确的数值约束，保证任意面板组合下界面都不会挤坏。

`[配图：pr-panel-widths-default.png、pr-panel-widths-fileviewer.png]`

另一处系统性思维的体现，是文件夹结构的信息架构：Abschnitt（区段）→ House（栋）→ Floor（楼层）的三级树状结构，直接贴合建筑工程项目本身的空间层级，而不是套用一个通用的"文件夹/子文件夹"模板。

`[配图：pr-structure-tree.png]`

---

## 板块 6｜影响力与结果

**已确认（2026-08-17）**：已落地到代码，无需配图。

**定性信号**：2026 年 7 月，客户方 Wolfgang Schmid 主动写信给 Herr Ripperda，申请提前使用 PR 的早鸟资格——在产品还处于分阶段交付节奏里的时候，已经有真实客户主动来要。

**可用性提升**：Upload 流程经过两轮测试迭代，从第一轮多个参与者卡在属性填写和上传流程，到第二轮 4/4 参与者顺利完成核心任务（具体数据见板块4）。

**后续可补充的量化数据**（暂不在本轮案例里，留作以后有产品埋点数据后再加）：文档处理/查找时间缩短了多少、上线后的采用率，方案见 `2(2).数据收集方法.md`。

---

## 下一步

**已确认（2026-08-17）**：六个板块全部已落地到 `data/projects.ts` + `app/work/[slug]/page.tsx`，并在浏览器中逐板块验证过渲染效果（含板块4结尾的操作录屏，已在普通 Chrome 窗口——而非自动化 tab——确认可以正常播放）。PR 案例的实现工作至此完成。

后续如果还要打磨，可以考虑：全文语气和长度再过一遍（尤其板块3的功能表格，是否要保留表格形式还是改成更视觉化的网格）；板块6"后续可补充的量化数据"如果之后拿到产品埋点数据，再回来补充具体数字。
